// main loop: subscribe AuctionOpen → run bid window → sign → submit
import * as dotenv from 'dotenv';
dotenv.config();

async function main() {
    console.log("AVS Operator Starting...")
}

main().catch(console.error);