# wagmi/viem ship conflicting picomatch peer ranges; don't fail the install on them (the npm
# equivalent was --legacy-peer-deps). auto-install-peers pulls in missing peers automatically.
auto-install-peers=true
strict-peer-dependencies=false
